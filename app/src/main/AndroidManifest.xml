<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SQLiteMasterV10"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".test.android_lifecycle.a.MainActivity"/>
        <activity android:name=".test.android_lifecycle.View_model.MainActivity"/>
        <activity android:name=".test.android_lifecycle.live_data.MainActivity"/>
        <activity android:name=".android_lifecycle.room.MainActivity"/>
        <activity android:name=".android_lifecycle.room.NewNoteActivity"/>
        <activity android:name=".test.room_test.MainActivity"/>
        <activity android:name=".test.room_test.NewNoteActivity"/>

        <activity android:name=".room.MainActivity"/>

    </application>

</manifest>